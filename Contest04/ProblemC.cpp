#include <bits/stdc++.h>

using namespace std;

void solve() {
    string s; cin >> s;
    if(s.size() < 26) {
        cout << "-1" << endl;
        return;
    }
    for(int l = 0, r = )
}

int main()

{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}
